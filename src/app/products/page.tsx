import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

const ProductsPage = () => {
  return (
    <div>
      <h1>Página produto</h1>
      <Button>Clique aqui!</Button>
      <Input placeholder="Boraaa" />
    </div>
  );
};

export default ProductsPage;
